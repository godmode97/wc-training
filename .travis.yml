language: node_js
sudo: true
os: osx
scripts:
  - cd todo-application
  - npm install
  - npm run build
deploy:
  provider: pages
  skip-cleanup: true
  github-token: $GITHUB_TOKEN
  keep-history: true
  on:
    branch: master