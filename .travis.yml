language: node_js
sudo: true
os: osx
  - "8"
before_install:
  - npm install -g npm@6.4.1
cache: npm
scripts:
  - cd todo-application
  - npm install
  - npm run build
deploy:
  provider: pages
  skip-cleanup: true
  github-token: $GITHUB_TOKEN
  keep-history: true
  on:
    branch: master